-- Клиентский портал КП сейчас не хранит ссылку на сделку (project_id) — только
-- снапшот полей на момент создания. Для read-only ICS-фида дедлайна сделки клиенту
-- (см. calendar-feed?portal=<id>) нужно знать, из какого проекта агентства брать
-- живой дедлайн. Колонка nullable — старые уже созданные порталы просто не будут
-- давать событий в фиде (клиент со старой ссылкой пересоздаст её при необходимости).
ALTER TABLE public.client_portals ADD COLUMN IF NOT EXISTS project_id UUID;
