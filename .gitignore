# Секреты — никогда не коммитить
client_secret_*.json
*.pem
*.key
.env
.env.*

# OS / редакторы
.DS_Store
Thumbs.db
.vscode/

# QA-артефакты локальных Playwright-тестов (tests/run.js)
tests/screenshots/

# Разовые ручные пробники и скриншотилки. В regression-suite не входят
# (tests/run.js гоняет только tests/suites/*), но остаются локально под рукой.
# Если проверка из пробника нужна навсегда — её место в tests/suites/, а не здесь.
tests/*-shot.js
tests/*-probe.js
tests/sidebar-iso.html
